# __package-name__

A short description of your plugin.

## How to install

See [the documentation](https://docs.inkdrop.app/manual/extend-inkdrop-with-plugins) for more detail.

```sh
ipm install __package-name__
```

## How to use

Run the `__package-name__:toggle` command (bound to <kbd>Ctrl</kbd>+<kbd>Alt</kbd>+<kbd>O</kbd> by default,
or available from the **Plugins** menu) to open the demo dialog.

## Development

```sh
npm install
npm run dev     # rebuild on change
npm run build   # production build into lib/
ipm link        # symlink into your Inkdrop data dir for local testing
```
